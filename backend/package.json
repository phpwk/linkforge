{
  "name": "linkforge-api",
  "version": "1.0.0",
  "description": "LinkForge application tier — REST API for a URL shortener",
  "main": "src/app.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --runInBand",
    "test:unit": "jest --runInBand test/links.unit.test.js",
    "test:integration": "jest --runInBand test/links.integration.test.js",
    "lint": "node --check src/app.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "prom-client": "^15.1.3",
    "nanoid": "^3.3.7"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
